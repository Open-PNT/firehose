[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name="aspn23_lcm"
version="0.1.0"
description="The LCM Python representation of ASPN-23"
dependencies=[
    "lcm"
]
requires-python = ">= 3.5"